# =============================================================================
# PetCam AI Server - 테스트 의존성
# =============================================================================
#
# 이 파일은 테스트 실행에 필요한 추가 패키지들입니다.
#
# 설치 방법:
#     pip install -r requirements.txt
#     pip install -r requirements-test.txt
#
# 또는 한 번에:
#     pip install -r requirements.txt -r requirements-test.txt
# =============================================================================

# -----------------------------------------------------------------------------
# pytest - 테스트 프레임워크
# -----------------------------------------------------------------------------
# Python에서 가장 많이 쓰이는 테스트 도구
pytest>=7.4.0

# -----------------------------------------------------------------------------
# pytest-asyncio - 비동기 테스트 지원
# -----------------------------------------------------------------------------
# async/await 함수를 테스트할 수 있게 해줌
# FastAPI는 비동기이므로 필수!
pytest-asyncio>=0.21.0

# -----------------------------------------------------------------------------
# pytest-cov - 테스트 커버리지 측정
# -----------------------------------------------------------------------------
# "전체 코드 중 몇 %가 테스트되었는지" 측정
# 예: 커버리지 70% = 코드의 70%가 테스트됨
pytest-cov>=4.1.0

# -----------------------------------------------------------------------------
# httpx - 비동기 HTTP 클라이언트
# -----------------------------------------------------------------------------
# FastAPI 앱에 테스트 요청을 보내는 도구
# requests와 비슷하지만 비동기 지원
httpx>=0.25.0

# -----------------------------------------------------------------------------
# aiosqlite - SQLite 비동기 드라이버
# -----------------------------------------------------------------------------
# 테스트용 메모리 데이터베이스 (PostgreSQL 대신 사용)
# 빠르고 가벼움, 테스트가 끝나면 자동 삭제
aiosqlite>=0.19.0

# -----------------------------------------------------------------------------
# ruff - 코드 린터 (빠른 속도)
# -----------------------------------------------------------------------------
# 코드 스타일 검사 도구 (flake8, pylint보다 훨씬 빠름)
ruff>=0.1.0
